/**
 * /customer-designs — direct URL route to the Customer Designs tab.
 * AppShell handles the actual content render; this page exists so the tab
 * is bookmarkable/shareable.
 */
export default function CustomerDesignsPage() {
  return null;
}
