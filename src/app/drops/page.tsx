/**
 * /drops — direct URL route for the Drop Planner. AppShell renders the
 * actual content based on its activeTab; this page is a marker so the
 * URL is bookmarkable.
 */
export default function DropsPage() {
  return null;
}
